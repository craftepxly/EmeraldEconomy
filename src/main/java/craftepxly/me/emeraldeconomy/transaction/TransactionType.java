package craftepxly.me.emeraldeconomy.transaction;

/**
 * TransactionType — enum representing the type of transaction.
 */
public enum TransactionType {
    // Player buys emerald from server (player pays money)
    BUY,
    // Player sells emerald to server (player receives money)
    SELL
}